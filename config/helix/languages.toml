[[language]]
name = "nix"
auto-format = true
formatter = {command = "nixfmt" }
language-servers = ["nixd"]
[language-server.nixd]
command = "nixd"

[[language]]
name = "python"
auto-format = true
formatter = {command = "ruff" , agrs = ["format", "-"] }
language-servers = ["pyright", "ruff"]
